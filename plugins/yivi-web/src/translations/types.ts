export interface Translations {
  header: string;
  helper: string;
  loading: string;
  button: string;
  qrCode: string;
  app: string;
  retry: string;
  back: string;
  cancelled: string;
  timeout: string;
  error: string;
  browser: string;
  success: string;
  cancel: string;
  pairing: string;
  pairingFailed: (code: string) => string;
}
